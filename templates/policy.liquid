<section class="policy-hero">
  <div class="container">
    <h1>{{ page.title }}</h1>
    <p>Clear, simple, and written with you in mind.</p>
  </div>
</section>

<section class="section-padding">
  <div class="container policy-content">
    {{ page.content }}
  </div>
</section>
